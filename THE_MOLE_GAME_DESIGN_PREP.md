# THE MOLE — 게임 기획 및 기술 준비 문서

> **ARCHIVED SNAPSHOT — v0.1**  
> 이 파일은 분할 전 원문을 보존하는 비권위 스냅샷이다. 현재 기준 문서와 탐색 순서는 [docs/README.md](./docs/README.md)를 따른다. 아래 기존 내용은 비교와 변경 이력 확인을 위해 유지한다.

> 상태: 초기 기획 / 데모 개발 준비  
> 목적: Codex를 활용한 웹 기반 1대1 턴제 첩보 게임 프로토타입 개발  
> 주의: 이 문서는 **현재까지 확정된 내용만 기록**한다. 아직 결정되지 않은 기획은 임의로 확정하지 않고 `TBD` 또는 빈칸으로 남긴다.

---

# 1. 프로젝트 개요

## 1.1 프로젝트명

- 가제: **THE MOLE**
- 최종 게임명: `TBD`

## 1.2 플랫폼

- PC 웹 브라우저
- 별도 설치 없이 링크로 접속
- 1대1 온라인 플레이

## 1.3 게임 장르

- 턴제 전략
- 첩보 / 정보전
- 심리전
- 추리
- 비대칭 정보 기반 멀티플레이

세부 장르 정의:

`TBD`

## 1.4 핵심 콘셉트

두 플레이어는 서로 적대하는 정보기관의 **국장** 역할을 맡는다.

각 국장은 하나의 도시 안에서 여러 명의 에이전트에게 명령을 내리고 첩보 작전을 수행한다.

각 조직 내부에는 상대편이 심어놓은 **배신자(The Mole)** 가 존재한다.

플레이어는 동시에 다음 두 가지 문제를 해결해야 한다.

1. 상대 정보기관의 작전을 방해하고 자신의 목표를 달성한다.
2. 자신의 조직 내부에 존재하는 배신자가 누구인지 추리한다.

핵심 경험:

> 플레이어가 내린 명령이 실패했을 때, 그것이 단순한 확률적 실패인지, 상대의 방해인지, 내부 배신 때문인지 판단해야 한다.

---

# 2. 현재 확정된 핵심 게임 규칙

## 2.1 플레이 방식

- 1대1 멀티플레이
- 턴제
- 양 플레이어는 **동시에 명령을 입력**한다.
- 한쪽 플레이어의 명령은 상대에게 공개되지 않는다.
- 양측이 명령 입력을 완료하면 턴이 실행된다.
- 해당 턴의 모든 에이전트 행동은 하나의 실행 단계에서 처리된다.
- 플레이어는 실행 결과를 3D 맵에서 관찰한다.
- 실행이 종료되면 다음 턴의 명령 단계로 넘어간다.

기본 흐름:

```text
명령 입력 단계
↓
양측 명령 확정
↓
서버에서 턴 판정
↓
턴 이벤트 생성
↓
양측 클라이언트가 동일한 턴 실행 장면 재생
↓
결과 확인
↓
다음 턴
```

---

# 3. 국장 시스템

각 플레이어는 정보기관의 국장 역할을 한다.

국장은 에이전트를 직접 조작하지 않고 **명령을 내린다.**

## 3.1 기본 역할

- 에이전트에게 명령 전달
- 수집된 정보 확인
- 내부 배신자 추리
- 상대 조직의 작전 방해
- 미션 목표 달성

## 3.2 불신(Restrict) 기능

국장은 특정 에이전트를 **불신 대상으로 지정**할 수 있다.

현재 확정된 효과:

- 불신 대상으로 지정된 에이전트는 해당 턴에 명령을 수행할 수 없다.

추가 비용 / 횟수 제한:

`TBD`

한 턴에 지정 가능한 인원:

`TBD`

불신 기능의 추가 효과:

`TBD`

---

# 4. 에이전트 시스템

각 플레이어는 여러 명의 에이전트를 운용한다.

정확한 에이전트 수:

`TBD`

## 4.1 에이전트 기본 요소

각 에이전트는 최소한 다음 정보를 가진다.

- 고유 ID
- 소속 정보기관
- 현재 위치
- 생존 여부
- 현재 명령
- 배신자 여부
- 행동 수행 가능 여부

추가 능력치:

`TBD`

예상 후보:

- 첩보
- 전투
- 은신
- 해킹
- 사교
- 이동
- 기타

> 위 능력치는 아직 확정하지 않는다.

## 4.2 행동 실패

각 에이전트의 명령은 항상 성공하는 것이 아니다.

- 행동마다 실패 가능성이 존재한다.
- 실패 여부에는 랜덤 요소가 존재한다.
- 실패했다고 해서 반드시 배신자인 것은 아니다.
- 배신자의 방해로 인해 실패 확률이 변경될 수도 있다.

정확한 확률 계산식:

`TBD`

행동별 기본 성공률:

`TBD`

---

# 5. 배신자(The Mole) 시스템

각 조직에는 상대 플레이어가 심어놓은 배신자가 존재한다.

정확한 배신자 수:

- 현재 기본안: 조직당 1명
- 최종 확정: `TBD`

## 5.1 배신자 정보

배신자의 정체는:

- 해당 배신자를 심은 상대 국장은 알고 있다.
- 배신자가 속한 조직의 국장은 알 수 없다.

## 5.2 배신자 명령 조작

상대 국장은 자신의 배신자를 이용해 해당 에이전트의 명령을 일부 조작할 수 있다.

현재 개념적으로 가능한 조작:

- 명령 실패 확률 증가
- 행동 지연
- 목적지 변경
- 목표 변경
- 정보 전달
- 정상적으로 행동

정확한 조작 종류:

`TBD`

조작 비용:

`TBD`

조작 횟수 제한:

`TBD`

공작 포인트 등의 별도 자원 사용 여부:

`TBD`

## 5.3 배신자의 전략적 목적

배신자는 항상 작전을 방해할 필요가 없다.

상대 국장은 배신자를 정상적으로 행동시켜 신뢰를 쌓은 뒤 중요한 순간에 배신시키는 전략을 사용할 수 있다.

---

# 6. 에이전트 간 상호작용

에이전트들은 도시 안에서 서로 만날 수 있다.

현재 고려 중인 상호작용:

- 협력
- 감시
- 미행
- 정보 교환
- 상대 에이전트 제거
- 보호
- 기타

정확한 행동 목록:

`TBD`

## 6.1 협력

여러 에이전트가 같은 작전을 함께 수행할 수 있다.

협력 시 효과:

`TBD`

배신자가 협력 작전에 포함될 경우의 영향:

`TBD`

## 6.2 제거

에이전트는 다른 에이전트를 제거할 수 있다.

제거 방식:

`TBD`

성공 확률:

`TBD`

제거 실패 시 결과:

`TBD`

---

# 7. 도시 / 맵 구조

게임의 배경은 하나의 도시이다.

## 7.1 표현 방식

- 화면은 3D 기반
- 도시 건물과 에이전트 캐릭터를 3D로 표현
- 에이전트가 실제 도시를 이동하는 모습을 보여준다.

## 7.2 내부 게임 로직

3D 공간의 실제 좌표를 게임 규칙의 핵심 상태로 사용하지 않는다.

게임 로직상 각 에이전트는 **지역(Location / Node)** 단위로 위치를 가진다.

예:

```text
Agent A
location = BANK
```

도시는 여러 개의 지역 노드와 연결 관계로 구성한다.

예시 구조:

```text
[공항]
   │
[호텔]─[광장]─[대사관]
   │      │       │
[은행]─[역]───[통신국]
```

> 위 구조는 예시이며 실제 맵은 미정이다.

## 7.3 실제 장소 목록

- 장소 수: `TBD`
- 장소 이름: `TBD`
- 장소별 기능: `TBD`
- 이동 가능 연결 관계: `TBD`

---

# 8. 미션 / 승리 조건

구체적인 게임 미션은 아직 확정하지 않는다.

## 8.1 기본 미션 구조

플레이어는 도시에서 에이전트들에게 명령을 내려 특정 첩보 목표를 달성한다.

미션 유형:

`TBD`

## 8.2 가능한 목표 후보

아래 항목은 아이디어 후보이며 확정 사항이 아니다.

- 기밀 문서 확보
- 특정 인물 확보
- VIP 보호
- VIP 납치
- 데이터 탈취
- 정보 수집
- 상대 조직 무력화
- 특정 대상 제거
- 제한 턴 내 목표 수행
- 물건 확보 후 대사관 / 탈출 지점까지 운반

## 8.3 승리 조건

메인 승리 조건:

`TBD`

보조 승리 조건:

`TBD`

점수제 사용 여부:

`TBD`

여러 승리 방식 사용 여부:

`TBD`

최대 턴 수:

`TBD`

---

# 9. 스토리 / 세계관

## 9.1 배경 시대

`TBD`

## 9.2 도시

도시명:

`TBD`

실존 도시 / 가상 도시:

`TBD`

## 9.3 양측 정보기관

정보기관 A:

`TBD`

정보기관 B:

`TBD`

## 9.4 전체 스토리

`TBD`

## 9.5 분위기

`TBD`

예상 후보:

- 냉전 첩보물
- 현대 정보전
- 근미래 첩보전
- 코믹 첩보물
- 느와르
- 기타

---

# 10. 한 턴의 처리 구조

턴은 크게 세 단계로 나눈다.

## 10.1 Phase 1 — Planning

양측 국장이 동시에 명령을 내린다.

예:

```text
Agent A → 이동
Agent B → 조사
Agent C → 미행
Agent D → 침투
Agent E → 감시
```

각 플레이어가 명령을 확정하면 Ready 상태가 된다.

## 10.2 Phase 2 — Resolve

두 플레이어가 모두 Ready가 되면 서버가 턴을 계산한다.

예상 처리 순서:

```text
명령 검증
↓
불신 대상 처리
↓
배신자 조작 처리
↓
이동
↓
감시 / 미행
↓
미션 행동
↓
랜덤 성공 판정
↓
전투 / 제거
↓
정보 획득
↓
승리 조건 확인
↓
TurnEvent 생성
```

> 세부 실행 순서는 밸런스와 충돌 처리 규칙에 따라 변경 가능하다.

## 10.3 Phase 3 — Playback

서버가 계산한 결과를 두 클라이언트가 받아 동일한 턴 실행 장면을 재생한다.

플레이어는 실행 단계에서 직접 캐릭터를 조작하지 않는다.

실행 종료 후 결과를 확인하고 다음 턴으로 넘어간다.

---

# 11. 턴 이벤트 시스템

게임 판정과 3D 연출을 분리한다.

서버는 턴 결과를 `TurnEvent[]` 형태로 생성한다.

예:

```json
[
  {
    "sequence": 1,
    "type": "MOVE",
    "agentId": "agent_a",
    "from": "BANK",
    "to": "HOTEL"
  },
  {
    "sequence": 2,
    "type": "INVESTIGATE",
    "agentId": "agent_a",
    "target": "HOTEL",
    "result": "SUCCESS"
  }
]
```

클라이언트는 이 이벤트를 순서대로 재생한다.

## 원칙

```text
Game Logic
↓
Game State
↓
TurnEvent[]
↓
3D Animation / UI / Sound
```

3D 오브젝트의 현재 Transform을 기반으로 게임 규칙을 결정하지 않는다.

---

# 12. 기술 스택

## 12.1 기본 스택

- Language: **TypeScript**
- Frontend: **Next.js**
- UI: **React**
- 3D: **React Three Fiber + Three.js**
- 3D Asset Format: **GLB / glTF**
- Repository: **GitHub**
- Hosting / Deployment: **Vercel**
- Database: **Supabase PostgreSQL**
- Authentication: **Supabase Anonymous Auth**
- Realtime: 초기에는 Polling, 필요 시 Supabase Realtime

## 12.2 개발 목표

현재 목표는 **데모**이다.

대규모 서비스 수준의 서버 구조는 구현하지 않는다.

---

# 13. 네트워크 구조

이 게임은 실시간 액션 게임이 아니다.

따라서 FPS처럼 플레이어 위치를 매 프레임 서버와 동기화하지 않는다.

서버는 다음 종류의 데이터만 관리한다.

- 게임 방
- 플레이어
- 에이전트 상태
- 명령
- 배신자 정보
- 랜덤 판정
- 턴 결과
- 승리 상태

클라이언트는 서버에서 받은 턴 결과를 이용해 3D 장면을 재생한다.

구조:

```text
Player A ── 명령 ──┐
                    │
                    ▼
                 Server
                    ▲
                    │
Player B ── 명령 ──┘

두 플레이어 Ready
        ↓
resolveTurn()
        ↓
TurnEvent[]
        ↓
Supabase 저장
        ↓
Player A / Player B
        ↓
동일한 턴 장면 재생
```

---

# 14. 서버 구조

별도의 전용 게임 서버를 운영하지 않는다.

서버 역할은 다음 조합으로 처리한다.

- Vercel Functions / Next.js Route Handler
- Supabase Database

중요한 게임 판정은 클라이언트가 아니라 서버에서 수행한다.

## 서버에서 처리해야 하는 것

- 명령 유효성 검증
- 양 플레이어 Ready 확인
- 배신자 조작
- 랜덤 판정
- 전투 / 미션 판정
- 게임 상태 갱신
- 승리 조건 판정
- TurnEvent 생성

---

# 15. 비밀 정보 처리

배신자 정보와 상대 플레이어의 비밀 명령은 클라이언트에 불필요하게 전달하지 않는다.

예:

```text
SERVER

Agent C
moleOwner = Player B
```

Player A에게는:

```text
Agent C
role = UNKNOWN
```

Player B에게는 필요에 따라:

```text
Agent C
role = MY_MOLE
```

를 제공한다.

## 원칙

화면에서 숨기는 것만으로 비밀 정보를 보호했다고 간주하지 않는다.

브라우저 개발자 도구에서 확인할 수 있는 데이터는 플레이어가 볼 수 있는 정보로 간주한다.

---

# 16. 랜덤 처리

행동 성공 여부의 랜덤 판정은 서버에서 수행한다.

예:

```text
기본 성공률
+
에이전트 보정
+
환경 보정
+
협력 보정
+
배신자 방해
=
실제 성공률
```

정확한 공식:

`TBD`

랜덤 생성 방식:

`TBD`

데모 단계에서는 서버 측 난수 판정으로 충분하다.

---

# 17. 게임 방 구조

## 17.1 목표

회원가입 없이 링크를 공유해 두 명이 접속할 수 있도록 한다.

예:

```text
https://example.vercel.app/game/ROOM_ID
```

## 17.2 흐름

```text
메인 화면
↓
게임 만들기
↓
Room 생성
↓
URL 생성
↓
상대에게 링크 공유
↓
상대 접속
↓
Player 1 / Player 2 배정
↓
게임 시작
```

정확한 방 ID 형식:

`TBD`

비밀번호 기능:

`TBD`

관전자:

`TBD`

---

# 18. 인증

데모 단계에서는 복잡한 회원가입 시스템을 만들지 않는다.

예정:

- Supabase Anonymous Auth
- 브라우저별 익명 사용자 ID

닉네임 입력 기능:

`TBD`

계정 저장:

현재 데모 범위에서는 우선순위 낮음.

---

# 19. 데이터 모델 초안

> 실제 스키마는 구현 시 변경 가능하다.

## 19.1 games

```text
id
status
turn
player_a_id
player_b_id
winner
created_at
updated_at
```

## 19.2 agents

```text
id
game_id
owner_id
location_id
alive
can_act
mole_owner_id
```

추가 능력치:

`TBD`

## 19.3 orders

```text
id
game_id
turn
player_id
agent_id
action_type
target_id
payload
created_at
```

## 19.4 turns

```text
id
game_id
turn_number
player_a_ready
player_b_ready
status
resolved_at
```

## 19.5 events

```text
id
game_id
turn_number
sequence
event_type
actor_id
target_id
payload
visibility
```

---

# 20. API 초안

실제 경로와 세부 구조는 변경 가능하다.

```text
POST /api/game/create
POST /api/game/join
GET  /api/game/state

POST /api/game/submit-orders
POST /api/game/ready

POST /api/game/resolve-turn
GET  /api/game/turn-events
```

## 주의

`resolve-turn`은 임의의 클라이언트가 중복 실행하여 같은 턴을 여러 번 처리하지 못하도록 보호해야 한다.

구현 방식:

`TBD`

---

# 21. 실시간 동기화

## 초기 데모

가장 단순한 방식부터 구현한다.

```text
Polling
```

클라이언트가 일정 주기로 게임 상태를 확인한다.

예:

```text
GET /api/game/state
```

상대가 아직 명령 입력 중이면 대기한다.

턴이 Resolve되면 결과를 가져와 재생한다.

## 이후 개선

필요할 경우:

```text
Supabase Realtime
```

로 변경한다.

현재 데모에서는 직접 WebSocket 서버를 운영하지 않는다.

---

# 22. 3D 구현

## 22.1 렌더링

React Three Fiber를 사용한다.

## 22.2 에셋

예상 구조:

```text
/public/models
/public/textures
/public/audio
```

3D 모델 포맷:

```text
.glb
```

## 22.3 캐릭터 이동

게임 로직상의 이동:

```text
BANK → HOTEL
```

클라이언트 표현:

```text
BANK의 3D 위치
↓
이동 경로
↓
HOTEL의 3D 위치
```

실제 이동 연출 방식:

`TBD`

후보:

- 직선 이동
- Waypoint 이동
- 사전 정의된 길
- 간단한 Pathfinding

## 22.4 애니메이션

필요 애니메이션:

`TBD`

예상 후보:

- Idle
- Walk
- Run
- Investigate
- Attack
- Hit
- Death
- 기타

---

# 23. UI 구조

현재 확정된 UI는 없다.

필요한 주요 화면 후보:

- 메인 화면
- 방 생성 / 참가
- 게임 화면
- 에이전트 목록
- 명령 선택 패널
- 턴 상태
- 결과 보고
- 승리 / 패배 화면

세부 UI 디자인:

`TBD`

---

# 24. 예상 프로젝트 구조

```text
the-mole/
│
├─ app/
│   ├─ page.tsx
│   │
│   ├─ game/
│   │   └─ [roomId]/
│   │       └─ page.tsx
│   │
│   └─ api/
│       └─ game/
│           ├─ create/
│           ├─ join/
│           ├─ state/
│           ├─ submit-orders/
│           └─ resolve-turn/
│
├─ components/
│   ├─ game/
│   │   ├─ CityScene.tsx
│   │   ├─ Agent.tsx
│   │   ├─ Building.tsx
│   │   └─ GameCamera.tsx
│   │
│   └─ ui/
│       ├─ AgentPanel.tsx
│       ├─ OrderPanel.tsx
│       └─ TurnPanel.tsx
│
├─ lib/
│   ├─ game/
│   │   ├─ resolveTurn.ts
│   │   ├─ movement.ts
│   │   ├─ combat.ts
│   │   ├─ mission.ts
│   │   └─ mole.ts
│   │
│   └─ supabase/
│
├─ public/
│   ├─ models/
│   ├─ textures/
│   └─ audio/
│
├─ types/
│   └─ game.ts
│
└─ docs/
    └─ GAME_DESIGN.md
```

> 실제 폴더 구조는 개발 중 변경 가능하다.

---

# 25. 핵심 코드 모듈

## GameState

게임의 현재 상태를 나타낸다.

예:

```ts
interface GameState {
  gameId: string;
  turn: number;
  phase: string;
  players: unknown[];
  agents: unknown[];
  mission: unknown;
}
```

정확한 타입:

`TBD`

## resolveTurn()

게임의 핵심 서버 로직.

책임:

1. 명령 검증
2. 배신자 조작
3. 이동 처리
4. 행동 판정
5. 랜덤 처리
6. 에이전트 간 상호작용
7. 게임 상태 갱신
8. 승리 조건 검사
9. TurnEvent 생성

가능하면 **순수 게임 로직과 DB 접근 코드를 분리**한다.

---

# 26. 개발 원칙

## 26.1 게임 로직과 3D 표현 분리

금지:

```text
3D 캐릭터의 Transform을 읽어
게임 결과를 직접 판정
```

권장:

```text
GameState
↓
Rule Resolver
↓
TurnEvent
↓
3D 연출
```

## 26.2 서버 권위

다음 값은 서버를 기준으로 한다.

- 턴
- 위치
- 생존 상태
- 명령 결과
- 배신자
- 랜덤 결과
- 승리 여부

## 26.3 클라이언트 책임

- UI
- 명령 입력
- 3D 렌더링
- 애니메이션
- 턴 결과 연출
- 사운드

## 26.4 데모 우선

현재 목표는 완제품이 아니라 게임의 핵심 재미 검증이다.

따라서 다음 기능은 우선 구현하지 않는다.

- 회원가입 UI
- 친구 시스템
- 공개 매치메이킹
- 랭킹
- 채팅
- 관전
- 복잡한 재접속
- 전용 게임 서버
- 실시간 Transform 동기화
- 대규모 동시 접속 대응
- 고급 Anti-Cheat
- 복잡한 3D 물리
- 고급 NavMesh 시스템

---

# 27. 데모 MVP

## 반드시 필요한 기능

- [ ] Next.js 프로젝트 생성
- [ ] Vercel 배포
- [ ] Supabase 연결
- [ ] 익명 사용자 식별
- [ ] 게임 방 생성
- [ ] 링크를 통한 두 번째 플레이어 참가
- [ ] Player 1 / Player 2 구분
- [ ] 게임 상태 생성
- [ ] 에이전트 데이터 생성
- [ ] 배신자 배정
- [ ] 3D 도시 씬 표시
- [ ] 3D 에이전트 표시
- [ ] 에이전트 선택
- [ ] 명령 선택
- [ ] 명령 서버 제출
- [ ] 양 플레이어 Ready 처리
- [ ] 서버 `resolveTurn()` 실행
- [ ] 랜덤 행동 성공 / 실패
- [ ] 배신자 조작 최소 1종 구현
- [ ] 불신 기능 구현
- [ ] TurnEvent 생성
- [ ] 두 클라이언트가 동일 결과 재생
- [ ] 다음 턴 진행
- [ ] 최소 1개의 미션
- [ ] 최소 1개의 승리 조건
- [ ] 게임 종료 화면

---

# 28. 초기 개발 순서

## Phase 1 — 프로젝트 기반

1. Next.js + TypeScript 프로젝트 생성
2. GitHub Repository 연결
3. Vercel 배포
4. Supabase 프로젝트 연결
5. 환경 변수 구성

## Phase 2 — 싱글 브라우저 게임 로직

1. GameState 정의
2. Agent 정의
3. Location 정의
4. Order 정의
5. TurnEvent 정의
6. `resolveTurn()` 기본 구현
7. 랜덤 성공 / 실패 구현

> 멀티플레이보다 게임 규칙을 먼저 테스트한다.

## Phase 3 — 3D

1. React Three Fiber 설치
2. 테스트 도시 생성
3. Agent 모델 표시
4. 노드 간 이동
5. TurnEvent 기반 애니메이션 재생

## Phase 4 — 멀티플레이

1. 방 생성
2. 방 참가
3. Player 배정
4. 명령 제출
5. Ready
6. 양측 Ready 확인
7. Turn Resolve
8. 결과 동기화

## Phase 5 — THE MOLE 핵심 기능

1. 배신자 배정
2. 비밀 정보 분리
3. 명령 조작
4. 불신
5. 배신 / 실패 추리 구조 테스트

## Phase 6 — 첫 미션

미션 내용:

`TBD`

---

# 29. Codex 작업 규칙

Codex는 이 문서를 현재 프로젝트의 기획 / 기술 기준으로 사용한다.

## 반드시 지킬 것

1. `TBD`로 표시된 게임 기획을 임의로 확정하지 않는다.
2. 새로운 게임 규칙이 필요하면 코드에 하드코딩하기 전에 사용자에게 결정이 필요하다는 점을 남긴다.
3. 게임 로직과 3D 표현 코드를 분리한다.
4. 비밀 정보는 클라이언트에 불필요하게 전달하지 않는다.
5. 중요한 게임 판정은 서버 측에서 처리한다.
6. 데모 범위 밖의 대규모 시스템을 임의로 추가하지 않는다.
7. 기능을 가능한 작은 단위로 구현한다.
8. 각 기능은 최소한의 테스트 가능한 상태까지 만든다.
9. 불필요한 추상화나 복잡한 아키텍처를 피한다.
10. 기존 구조를 변경할 경우 변경 이유를 문서화한다.

## 개발 시 우선순위

```text
게임이 실제로 동작하는가
>
두 플레이어 결과가 일치하는가
>
게임 규칙이 올바른가
>
코드 구조가 유지보수 가능한가
>
그래픽 완성도
```

---

# 30. 아직 결정해야 할 핵심 기획

아래 항목은 개발 전 또는 개발 중 추가 논의가 필요하다.

## 게임 기본

- 최종 게임명:
- 한 게임의 평균 시간:
- 최대 턴:
- 에이전트 수:
- 배신자 수:

## 에이전트

- 능력치 종류:
- 개별 특성:
- 행동 목록:
- 행동 성공률:
- 제거 규칙:
- 협력 규칙:

## 배신자

- 조작 가능한 행동:
- 조작 비용:
- 조작 횟수:
- 배신자 공개 / 추리 방식:
- 배신자 체포 / 제거 방식:

## 국장

- 불신 사용 횟수:
- 추가 국장 능력:
- 자원 시스템 여부:

## 맵

- 지역 수:
- 지역 종류:
- 이동 규칙:
- 이동 거리:
- 특수 시설:

## 미션

- 첫 번째 미션:
- 메인 목표:
- 서브 목표:
- 승리 조건:
- 패배 조건:
- 점수 시스템 여부:

## 정보 시스템

- 어떤 정보를 상대에게 공개할지:
- 어떤 정보를 숨길지:
- 실패 원인의 공개 수준:
- 감시 / 미행 결과의 정보량:

## 스토리

- 시대:
- 세계관:
- 도시:
- 정보기관:
- 분위기:
- 캐릭터 이름:

## 그래픽

- 아트 스타일:
- 카메라 방식:
- 캐릭터 스타일:
- 도시 스타일:
- 색상:
- UI 스타일:

---

# 31. 현재 가장 중요한 검증 질문

초기 데모에서는 다음 질문에 답하는 것을 가장 중요한 목표로 한다.

1. 동시에 명령을 내리고 결과를 관찰하는 방식이 재미있는가?
2. 명령 실패가 단순 스트레스가 아니라 추리 요소로 작동하는가?
3. 배신자의 존재가 실제로 플레이어의 판단을 흔드는가?
4. 배신자를 의심하고 불신하는 과정에 의미 있는 선택이 발생하는가?
5. 상대 전략과 내부 배신자를 동시에 고려하는 것이 과도하게 복잡하지 않은가?
6. 턴 실행 장면을 보는 시간이 지루하지 않은가?
7. 3D 연출이 정보 전달과 긴장감에 실제로 도움이 되는가?
8. 다시 플레이했을 때 다른 상황이 발생하는가?

---

# 32. 문서 관리 원칙

게임 기획이 확정될 때마다 이 문서를 갱신한다.

권장 표기:

```text
CONFIRMED
현재 확정된 내용

TBD
아직 결정하지 않은 내용

CANDIDATE
후보 아이디어

DEPRECATED
폐기된 과거 기획
```

기획 변경 시 기존 내용을 무조건 삭제하기보다 중요한 변경 사항은 별도 Change Log에 기록한다.

---

# 33. Change Log

## v0.1

- 초기 기획 / 기술 준비 문서 작성
- 1대1 웹 기반 턴제 구조 정의
- 동시 명령 / 동시 실행 구조 정의
- 배신자 기본 개념 정의
- 서버 권위 방식 정의
- GitHub + Vercel + Supabase 기반 기술 스택 정의
- 게임 로직 / 3D 연출 분리 원칙 정의

다음 변경:

`TBD`
